{% extends 'base.html.twig' %}

{% block body %}
    <div class="d-none d-md-block">

        <div class="row d-flex justify-content-center">

{#            <div class="mt-5">#}

                {{ form_start(itemForm) }}
                {{ form_widget(itemForm) }}
                <button type="submit">Go !</button>
                {{ form_end(itemForm) }}

{#            </div>#}



        </div>

    </div>
{% endblock %}

{% block title %}{{ parent() }} | Add an Item{% endblock %}

